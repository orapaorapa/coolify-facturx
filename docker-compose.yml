version: '3.8'

services:
  facturx:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: facturx
    volumes:
      - /data/factures:/data
    restart: unless-stopped